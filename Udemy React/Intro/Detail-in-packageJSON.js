Public Folder : all those files that our browser needs an older version of JS and HTML to understand,
            folder where all is gonna go.
src folder: where our code located

"scripts": {
    "start": "react-scripts start",
    // 

    "build": "react-scripts build",
    //turn all of the react code in our source folder into the version that browser understad, put
    // it in inside the public folder.

    "test": "react-scripts test",
    // run the test code that we're gonna write

    "eject": "react-scripts eject"
    // take out all of the configuration files that is hidden from us for Babylon web hack in case
    // we wanted to managed it ourselves.
}

YARN and NPM are a package manager. What differentiate is the command you put in terminal.
yarn create by facebook when NPM has few issues. 