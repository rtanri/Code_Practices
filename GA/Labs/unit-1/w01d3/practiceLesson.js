// first practice
let sum = (x,y) => {
    // if we only use it 1-time, just use x+y
    return x + y;

    // using variable result multiple times in other logic within the function
    // let result = x+y
    // return result;
}
let z = sum(1,5)
console.log(z);

// second practice