// Write a function call sum using arrow syntax that accepts two inputs, x and y, and returns the summation result of the input
// Call the function and store the result in variable z
// Print the value of variable z via console.log

let sum = (x, y) => {
    let result = x + y
    return result
}

let z = sum(10, 10)

console.log(z)
